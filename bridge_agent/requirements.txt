websockets>=12.0
pyscard>=2.0.7       # PC/SC NFC reader (optional — graceful fallback if absent)
cryptography>=41.0
# Optional — for Zebra ZPL conversion and high-quality card rendering
# PyMuPDF>=1.23.0

# Build tools (for packaging as executable)
# pyinstaller>=6.0
